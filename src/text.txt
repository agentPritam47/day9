make a three js slider which will be scrolling in z axis and the plane will be position equally in thr right and left side the image plane will have the width and height (aspect ratio) of the actual image use gsap scrolltrigger for animating the camera in z direction and for each image plane there will be html div having heading and escription of the active image also add a fog to the scene create a var for the no of planes and create a shader material in which it will have a uniform which will be called velocity which will be upfdated by lenis on scroll and in the vertex shader we will bend the plane from the center using the distamce function in the z direction and the direction of the bending (-z and +z) will be based on the scrolling direction